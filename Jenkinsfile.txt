pipeline {
	agent any
	stages {
		stage('Checkout SCM') {
			steps {
				git '/home/workspace/enkinsDependencyCheckTest_master'
			}
		}

		stage('OWASP DependencyCheck') {
			steps {
				dependencyCheck additionalArguments: '--format HTML --format XML', odcInstallation: 'jenkins-practice'
			}
		}
	}	
	post {
		success {
			dependencyCheckPublisher pattern: 'dependency-check-report.xml'
		}
	}
}